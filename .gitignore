# Never commit datasets, seeds, or raw benchmark output
*.ndjson
# seed summaries live outside the repo; scenario manifests and
# compare receipts are EVIDENCE and must never be ignored
*.summary.json
evidence/raw/
*.log

# Credentials — must never exist here at all
*.key
*.pem
service-account*.json
credentials*.json
.env

__pycache__/
*.pyc
.DS_Store
.block-pinned/
